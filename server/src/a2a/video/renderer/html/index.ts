/**
 * HTML Renderer Module
 *
 * Puppeteer-based HTML/CSS rendering for video compositions.
 */

export {
  renderHtmlFrame,
  closeBrowser,
  warmupBrowser,
  isPuppeteerAvailable,
  type HtmlRenderConfig,
} from './renderer.js';
