/**
 * iBird Store - Re-export Shim
 *
 * The canonical store now lives at src/applications/ibird/store.ts.
 * This file re-exports everything for backwards compatibility with
 * consumers outside the app bundle (pages, services, etc.).
 */
export * from '@/applications/ibird/store';
